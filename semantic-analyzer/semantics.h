#include <stdbool.h>
#include "../parser/analy_syn.h"
#include "utils.h"

bool identifierExists(LEX_CODE identifier);
bool hasAllPositiveNumbers(int number1, int number2, int number3);
bool isString(char *string);
bool isNumber(char *number);